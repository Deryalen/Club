using System;
using System.Collections.Generic;
using System.Linq;
using Club.RunningClubDataSetTableAdapters;

namespace Club
{
    static class Training
    {
        /*public static Dictionary<int, string> GetTrainings(int? participantId, ParticipantTableAdapter adapter, HeatTableAdapter heatAdapter)
        {
            if (participantId == null)
            {
                throw new ArgumentException("Участник не найден");
            }

            var participant = adapter.GetDataBy3(participantId.Value).Single();

            var popular = heatAdapter.GetDataBy4();
        }*/
    }
}
